name: aura_core
description: Core deterministic game loop and state models for A.U.R.A.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  meta: ^1.11.0
  http: ^1.2.0
  crypto: ^3.0.3
  archive: ^3.6.0
  cryptography: ^2.9.0

dev_dependencies:
  test: ^1.24.0
