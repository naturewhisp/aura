/// Modalità attiva della finestra desktop a runtime.
enum ActiveWindowMode {
  windowed,
  maximized,
  borderlessFullscreen,
}

/// Preferenza di avvio della finestra desktop.
enum WindowStartupMode {
  restorePrevious,
  windowed,
  maximized,
  borderlessFullscreen,
}
